// If using JSLint for syntax debugging, include the following two lines for better management
/*jslint browser: true, sloppy: true, white: true*/
/*global $, console, alert, app*/

$(function() { 
	app.init();
});

var app = { 
	init: function() { 
		console.log("Application Started");
	}
};